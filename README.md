<div dir="rtl">

# VS Code RTL

إضافة لـ VS Code تحوّل واجهته إلى وضع **من اليمين إلى اليسار (RTL)**، مبنيّة على
نظام **استراتيجيات قابل للتوسّع** يسهّل إضافة طرق جديدة لتطبيق RTL مستقبلًا.

## المزايا

- تبديل واجهة العمل إلى RTL بأمر واحد.
- إبقاء منطقة تحرير الشيفرة باتجاه LTR (اختياريّ) لتفادي قلب اتجاه الكود.
- نطاق قابل للضبط: واجهة العمل فقط أو كامل الواجهة.
- بنية قابلة للتوسّع: أضف استراتيجيّتك دون لمس بقيّة الإضافة.

## الأوامر

تُتاح من لوحة الأوامر (`Ctrl+Shift+P`):

| الأمر | الوصف |
| --- | --- |
| `RTL: تفعيل وضع اليمين-لليسار` | تطبيق RTL. |
| `RTL: تعطيل وضع اليمين-لليسار` | إزالة RTL. |
| `RTL: تبديل وضع اليمين-لليسار` | عكس الحالة الحالية. |
| `RTL: إعادة التطبيق` | يُستخدم بعد تحديث VS Code إن أُلغي الحقن. |

> بعد التفعيل/التعطيل ستُطلب إعادة تحميل النافذة لتطبيق التغييرات.

## الإعدادات

| الإعداد | الافتراضيّ | الوصف |
| --- | --- | --- |
| `vsCodeRtl.strategy` | `cssInjection` | استراتيجية التطبيق. |
| `vsCodeRtl.scope` | `workbench` | نطاق RTL: `workbench` أو `all`. |
| `vsCodeRtl.keepEditorLtr` | `true` | إبقاء المحرّر باتجاه LTR. |

## كيف تعمل

تعتمد استراتيجية `cssInjection` على حقن كتلة CSS مُعلَّمة في ملفّ واجهة العمل
داخل تثبيت VS Code. لذلك:

- تتطلّب صلاحيّة الكتابة على مجلّد التثبيت (قد تحتاج تشغيل VS Code كمسؤول).
- قد يُلغي تحديثُ VS Code الحقنَ، فيُعاد عبر أمر **إعادة التطبيق**.

## البنية

</div>

```
src/
├── extension.ts              # المدخل + تسجيل الاستراتيجيات
├── core/
│   ├── RtlStrategy.ts        # واجهة التوسّع (Strategy)
│   ├── StrategyRegistry.ts   # سجلّ الاستراتيجيات
│   └── RtlController.ts      # المنسّق
├── config/
│   └── Settings.ts           # قراءة الإعدادات
├── strategies/
│   ├── rtlCss.ts             # توليد/إزالة CSS (دوال نقيّة)
│   ├── workbenchPaths.ts     # تحديد ملفّ واجهة VS Code
│   └── CssInjectionStrategy.ts
└── test/
    ├── unit/                 # اختبارات وحدة سريعة
    └── suite/                # اختبارات تكامل داخل VS Code
```

<div dir="rtl">

## إضافة استراتيجية جديدة

1. أنشئ صنفًا يحقّق `RtlStrategy` في `src/strategies/`.
2. سجّله في `buildRegistry()` ضمن `src/extension.ts`.
3. أضف معرّفه إلى قائمة `vsCodeRtl.strategy` في `package.json`.

## التطوير

</div>

```bash
npm install
npm run compile      # ترجمة TypeScript
npm run lint         # فحص اللينتر
npm run test:unit    # اختبارات الوحدة (سريعة)
npm test             # اختبارات التكامل داخل VS Code
```

<div dir="rtl">

ثمّ اضغط `F5` لتشغيل نافذة تطوير مع الإضافة محمَّلة.

## الرخصة

[MIT](LICENSE)

</div>
